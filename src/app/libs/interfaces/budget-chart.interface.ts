export type Subgroup = 'nitrogen' | 'normal' | 'stress' | 'hard' | 'easy' | 'diff' | 'so' | 'then';

export interface BudgetChartData {
  nitrogen: string;
  group: string;
  normal: string;
  stress: string;
  hard: string;
  easy: string;
  diff: string;
  so: string;
  then: string;
}
